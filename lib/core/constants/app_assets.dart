/// Bundled asset paths, declared in `pubspec.yaml` under `flutter/assets`.
class AppAssets {
  const AppAssets._();

  static const String profilePhoto = 'assets/images/profile.jpg';

  /// Served relative to the web root, hence the doubled `assets/` prefix.
  static const String resumePdf = 'assets/assets/resume/Samarth_Adat_Resume.pdf';
}
